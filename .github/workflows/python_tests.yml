name: Python application test

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Cache Python environment
      id: cache-python-env
      uses: actions/cache@v3
      with:
        path: .venv
        key: ${{ runner.os }}-venv-${{ hashFiles('**/requirements.txt', '**/.python-version') }}
        restore-keys: |
          ${{ runner.os }}-venv-

    - name: Cache LaTeX installation
      id: cache-latex
      uses: actions/cache@v3
      with:
        path: /usr/share/texlive
        key: ${{ runner.os }}-latex-${{ hashFiles('**/report.tex') }}
        restore-keys: |
          ${{ runner.os }}-latex-

    - name: Install LaTeX dependencies (if not cached)
      if: steps.cache-latex.outputs.cache-hit != 'true'
      run: |
        sudo apt-get update
        sudo apt-get install -y texlive-latex-extra latexmk

    - name: Install Python environment
      if: steps.cache-python-env.outputs.cache-hit != 'true'
      run: |
        python -m venv .venv
        source .venv/bin/activate
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: Set up project environment
      run: |
        source .venv/bin/activate
        python ./src/settings.py  # Adjust path if necessary

    - name: Run doit
      run: |
        source .venv/bin/activate
        doit

    - name: Test with pytest
      run: |
        source .venv/bin/activate
        pytest
